{
  "name": "kappa-bot",
  "version": "1.0.0",
  "description": "A bot to set up and manage service events for the Nu Theta slack",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ezgi",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^3.12.2"
  }
}
